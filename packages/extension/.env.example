# URL of the Repruvia web app the extension opens after a recording.
# Optional override. When unset, dev builds use http://localhost:3000 and
# production builds use https://repruvia.github.io.
VITE_WEB_APP_URL=http://localhost:3000
